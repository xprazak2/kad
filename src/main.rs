use lunatic::{Mailbox, process::StartProcess};
// use tracing::info;

#[lunatic::main]
fn main(_: Mailbox<()>) {
    // tracing_subscriber::fmt::init();

    // let args = ("node".to_owned(), BinaryOptionNodeArgs{ id: Some(2), k: Some(3) });
    // info!("Starting node supervisor...");
    // NodeSupervisor::start_link(args, None);
}
